    begin
        prog   = irexec
        button = KEY_UP
        config = "amixer -M set Master 1+"
        repeat = 1
    end

    begin
        prog   = irexec
        button = KEY_DOWN
        config = "amixer -M set Master 1-"
        repeat = 1
    end

    begin
        prog   = irexec
        button = KEY_PLAY
        config = bash /root/spdif
    end
